# Copy to secrets/elastic-password (gitignored) after first bootstrap.
# Or set ElasticPassword in config.psd1.
# Show current value: python show_elastic_password.py
# Reset only when needed: python reset_elastic_password.py